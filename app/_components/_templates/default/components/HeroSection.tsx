import { HeroSectionI } from "@/types/userData"

const HeroSection = ({name, description, image,role} : HeroSectionI) => {
  return (
    <div className="h-[80lvh]" id="hero">hero</div>
  )
}
export default HeroSection